# SVOIP_Network
The network side of SVOIP

The goal of this is to provide a simple abstraction layer for communicating over a network

In other words, reinventing the wheel because I can.

Currently text is all that is supported, voice is next.
